<div class="jumbotron text-center">
  <h2>Edit your Proposal</h2>
  <br/>

  <div class="form-horizontal form-group form-group-lg row">
    <div class="row">
      <div class="col-xs-10 col-xs-offset-1 col-sm-10 col-sm-offset-1 col-md-10 col-md-offset-1">
         <label for="title">A terse and specific summary of the activity in one line (subject line)</label>
         {{input type="text" id="title" value=title class="form-control" placeholder="Please enter a title" autofocus="autofocus"}}
         <br/>
         <label from="value">Requested amount in € (estimated maximum) &mash; less than {{maxBudget}}€ for micro grants, up to 5000€ for small grants</label>
         {{input type="text" value=value id="value" class="form-control" placeholder=(concat "Requested amount in € (estimated maximum)")}}
         <br/>
         <label for="description">Summary and overall goals of the proposal</label>
         {{textarea value=description id="description" class="form-control" placeholder="Please enter a description" rows="5" autoresize="true"}}
         <br/>
         <label for="activities">List the specific activities and actions you will perform</label>
         {{textarea value=activities id="activities" class="form-control" placeholder="Please enter your activities/actions" rows="5" autoresize="true"}}
         <br/>
         <label for="recipient">Recipient name (individual or organization)</label>
         {{input type="text" value=recipient id="recipient" class="form-control" placeholder="Recipient name (individual or organization)"}}
         <br/>
         {{#if (eq isMicroBudget false)}}
         <label for="recipient2">For grants from 2000€: Second recipient/collaborator in another EU country</label>
         {{input type="text" value=recipient2 id="recipient2" class="form-control" placeholder="For small grants: Collaborator in second EU country"}}
         <br/>
         {{/if}}
         <label for="contact">Contact email address for this proposal (grantee) &mdash; only visible for RFF staff</label>
         {{input type="text" value=contact id="contact" class="form-control" placeholder="Recipient contact information"}}
         <br/>
         <label for="finisheddate">When do you expect the activities associated with this proposal to be over? When should we reach out for a report?</label>
         {{bootstrap-datepicker id="enddaypicker" value=finisheddate id="finisheddate" startDate=minimumProposalStartDate format="yyyy/mm/dd" autoclose=true class="form-control" placeholder="Expected end date"}}
		 <br/>
         <label for="startdate">When do you want the voting on this proposal to begin? (micro: daily, runtime 14 days; small: every 2 months for 2 weeks)</label>
         {{#if isMicroBudget}}
         {{bootstrap-datepicker id="daypicker" value=startdate id="startdate" startDate=minimumProposalStartDate format="yyyy/mm/dd" autoclose=true class="form-control" placeholder="Start date of the proposal vote"}}
         {{else}}
         {{bootstrap-datepicker id="monthpicker" value=startdate id="startdate" startDate=minimumProposalStartDate format="yyyy/mm" minViewMode=1 autoclose=true class="form-control" placeholder="Which month to run this proposal in"}}
         {{/if}}
      </div>
    </div>

    <br/>

    <div class="row">
      <div class="col-xs-10 col-xs-offset-1 col-sm-offset-7 col-sm-4 col-md-3 col-md-offset-8">
        <button disabled={{isDisabled}} {{action 'saveProposal'}} class="btn btn-primary btn-lg btn-block">Save proposal</button>
      </div>
    </div>
  </div>

  {{#if progressMessage}}
    <div class="alert alert-info">{{progressMessage}}</div>
  {{/if}}
  {{#if responseMessage}}
    <div class="alert alert-success">{{responseMessage}}</div>
  {{/if}}
  {{#if errorMessage}}
    <div class="alert alert-danger">{{errorMessage}}</div>
  {{/if}}

</div>
