<nav class="navbar navbar-inverse navbar-static-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{link-to 'Digital Whistleblowing Fund' 'index' class="navbar-brand"}}
    </div>

    <div class="collapse navbar-collapse" id="main-navbar">
      <ul class="nav navbar-nav">
            <li>{{link-to 'Details' 'details'}}</li>
            <li>{{link-to 'Contact' 'contact'}}</li>
            {{#if session.isAuthenticated}}
            <li>{{link-to 'How it works' 'how-it-works'}}</li>
            <li>{{link-to 'Proposals' 'proposals'}}</li>
            {{#if (not-eq currentUser.user.id "1")}}
              <li>{{#link-to 'proposals.new'}}<span class="glyphicon glyphicon-circle-arrow-right"></span> Submit New Proposal{{/link-to}}</li>
            {{/if}}
            {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
            {{#if (eq currentUser.user.id "1")}}
              {{#link-to 'admin' tagName="li"}}<a href="">Admin</a>{{/link-to}}
            {{/if}}
            {{#if session.isAuthenticated}}
              <li><a href="#" {{action 'invalidateSession'}}><span class="glyphicon glyphicon-log-in"></span> &nbsp;Logout
                  {{#if currentUser.user}}
                    ({{currentUser.user.email}})
                  {{/if}}
              </a></li>
            {{else}}
              <li>{{#link-to 'login'}}<span class="glyphicon glyphicon-log-out"></span> Login{{/link-to}}</li>
            {{/if}}
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
